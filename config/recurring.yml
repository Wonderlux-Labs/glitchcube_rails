default: &default
  conversation_timeout_monitor:
    class: Recurring::System::ConversationTimeoutMonitorJob
    schedule: every minute
    queue: default

  weather_forecast_update:
    class: Recurring::Content::WeatherForecastSummarizerJob
    schedule: "0 * * * *"     # Every hour at the top of the hour
    queue: default

  reflection:
    class: Recurring::Reflection::ReflectionJob
    schedule: "*/30 * * * *"  # Every 30 minutes — the consolidator (deep pass)
    queue: default

  artifact_decay:
    class: Recurring::Artifact::DecayJob
    schedule: "0 9 * * *"     # Daily at 9am — the amnesia loop (arithmetic, no LLM)
    queue: default

production:
  <<: *default

  # Production-specific jobs
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12

  host_registration:
    class: Recurring::System::HostRegistrationJob
    schedule: "*/5 * * * *"   # Every 5 minutes
    queue: default

development:
  <<: *default

  host_registration:
    class: Recurring::System::HostRegistrationJob
    schedule: "*/3 * * * *"   # Every 3 minutes in dev (faster)
    queue: default
